name: Smart Triggers

on:
  push:
    branches:
      - main
      - 'release/*'
    paths:
      - 'src/**'
      - 'app/**'

jobs:
  app-change:
    runs-on: ubuntu-latest

    steps:
      - name: Show trigger
        run: echo "Workflow triggered by a change in src/ or app/"
